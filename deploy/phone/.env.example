# Container image (GHCR)
APP_IMAGE=ghcr.io/fa72/mobianwebmonitor:latest

# Password hash generated by: dotnet run -- --generate-hash <password>
MONITOR_AUTH_HASH=

# Docker GID on the host: getent group docker | cut -d: -f3
DOCKER_GID=998
